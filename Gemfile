clear_sources
source "http://gemcutter.org"

only :bundle do
  gem "json"
  gem "nokogiri"
  gem "rack-client", ">=0.2.2"
end

only :test do
  gem "rake"
  gem "bundler", ">=0.7.0"
end

disable_system_gems
bin_path "gbin"
